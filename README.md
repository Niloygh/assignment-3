### 1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?
ans:  GetElementById using by We can only catch one with id . GetElementsByClassName using by We can any class catch . QuerySelector using by any id or any class catch just one id or one class . QuerySelectorAll using by ony id or all call catch.

### 2. How do you create and insert a new element into the DOM?
ans: first i am document.createElement() with to create new element and second this element appendChild() I will do it in a section.


### 3. What is Event Bubbling? And how does it work?
ans: An event propagation process where when an event occurs in a child element, that event moves up step by step to its parent → grandparent → document.





### 4. What is Event Delegation in JavaScript? Why is it useful?
na: many child element for of different event listener no using their common parent element this one event listener using and event bubbling by using event handle is done. Ans this is very useful.


### 5. What is the difference between preventDefault() and stopPropagation() methods?
ans: preventDefault() disables the browser's default behavior, while stopPropagation() prevents the event from going to the parent element.
---

